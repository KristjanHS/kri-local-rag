---
description: "Rules for Terminal Command Execution"
type: agent-select
globs: []
alwaysApply: false
---
- **Run commands from the project root.**
- **Use the `python` alias,** as the shell is configured to use the correct virtual environment.
- **Run Pytest as a module:** `python -m pytest tests/` to avoid `ImportError` issues.
- **NEVER use PYTHONPATH.** It can break virtual environment detection.
