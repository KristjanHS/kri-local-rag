---
description: "Handle validation and test failures gracefully with clear error reporting"
type: agent-select
globs: []
alwaysApply: false
---
- On initial validation (`jsonlint`) or test failures, stop further edits.
- This rule applies to first-attempt failures only. After 3 failed attempts, the problem-solving rule (see `problem-solving.mdc`) takes precedence.
- Output the failure reason and suggested next steps.
