# Auto-resolution allow-list for promote_dev_to_main.sh
#
# Each non-empty, non-comment line is a path prefix or file name.
# During merge conflicts, files matching any of these patterns can be
# automatically resolved by preferring the source branch.
#
# Example defaults:
README.md
docs/
docs_AI_coder/
