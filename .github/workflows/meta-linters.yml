name: Meta linters (workflows, YAML, Dockerfiles)

on:
  pull_request:
    paths:
      - ".github/workflows/**/*.yml"
      - ".github/workflows/**/*.yaml"
      - "**/*.yml"
      - "**/*.yaml"
      - "Dockerfile"
      - "docker/**"
  workflow_dispatch: {}

permissions:
  contents: read

concurrency:
  group: meta-linters-${{ github.ref }}
  cancel-in-progress: true

env:
  # CI runners are CPU-only — select the cpu torch extra so no CUDA wheels are
  # pulled. See docs/plans/2026-06-23-gpu-cpu-torch-extras.md.
  KRI_VARIANT: cpu

jobs:
  actionlint:
    name: Actionlint (workflows)
    if: ${{ github.actor != 'nektos/act' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run actionlint
        uses: docker://rhysd/actionlint:latest
        with:
          args: -color
  yamlfmt:
    name: "Yamlfmt (repo YAML)"
    if: ${{ github.actor != 'nektos/act' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Python + uv (cached)
        uses: ./.github/actions/setup-uv
      - name: Yamlfmt via Makefile
        run: make yamlfmt
  hadolint:
    name: Hadolint (Dockerfiles)
    if: ${{ github.actor != 'nektos/act' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run hadolint
        uses: hadolint/hadolint-action@v3.1.0
        with:
          dockerfile: docker/app.Dockerfile
          format: github
